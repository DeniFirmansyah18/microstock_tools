import 'package:flutter/material.dart';

class HomeTrendsScreen extends StatelessWidget {
  const HomeTrendsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('HomeTrendsScreen'),
      ),
    );
  }
}
